feature(resynth): rebuild trigger

This is a no-op documentation change used to trigger a rebuild of the
feature/resynthesis-engine branch on GitHub Actions. It appends a timestamp
so CI will pick up a new commit and produce fresh artifacts that include the
latest RTL for the resynthesis engine.

Timestamp: 2026-08-30T19:57:00Z
